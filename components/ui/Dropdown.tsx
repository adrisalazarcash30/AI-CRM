// Re-export the existing Dropdown so it can be imported from the
// canonical `components/ui/` path used in the design system.
export { default } from "../Dropdown";
export type { DropdownItem } from "../Dropdown";
